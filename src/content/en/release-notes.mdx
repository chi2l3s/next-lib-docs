---
title: Release Notes
description: "Short changelog for NextLib releases."
order: 21
---

# Release Notes

## v1.0.7

- Added advanced WHERE operators: `>`, `<`, `>=`, `<=`, `LIKE`, `IN`, `BETWEEN`
- Improved query scenarios for Dynamic Database
- Updated docs and examples

## Adding external release files

If new release documents appear, add them as MDX files under `src/content/<locale>/releases/` and link them from this page.
